# Cloudflare Pages — SPA fallback
# Reguła z "!" wymusza redirect (override automatic asset matching = no infinite loop)
# Functions /api/* są obsługiwane automatycznie przez functions/api/*

/*    /index.html   200!

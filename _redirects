# SPA fallback (Netlify + Cloudflare Pages)
# BEZ "!" — istniejące pliki statyczne (photos/, sw.js itd.) mają pierwszeństwo;
# wymuszona reguła 200! serwowała na Netlify index.html zamiast zdjęć.
# Functions /api/* są obsługiwane automatycznie przez functions/api/*

/*    /index.html   200
